# 当前项目需求文档

## 1. 文档说明

本文档基于当前仓库中已经体现出的产品行为整理，目标是帮助后续开发 Agent 理解“这个项目现在要做什么、已经做到什么、边界在哪里”。本文只描述功能点、业务规则、交互诉求和运行要求，不展开技术实现。

本文档描述的是“当前版本需求基线”，适合作为继续开发、重构或补全产品能力时的参考输入。

## 2. 产品定位

该项目是一个本地运行的 LLM 路由与管理平台，面向编码 Agent、IDE 插件或其他需要 OpenAI 兼容接口的调用方。

产品目标如下：

- 对外提供统一的 OpenAI 兼容调用入口，屏蔽上游 Provider 差异。
- 允许管理员在本地管理多个上游 Provider，并为每个模型配置独立的路由关系。
- 允许管理员以“模型优先”的方式控制不同 Provider 的使用顺序，而不是只维护一个全局默认 Provider。
- 提供调用统计、成本估算、延迟分析和错误审计能力，便于日常运维与排障。
- 在需要时可将 API 暴露到局域网，但后台管理能力仍仅限本机访问。

## 3. 角色与使用场景

### 3.1 管理员

管理员是当前机器上的本地使用者，负责：

- 配置上游 Provider 的名称、地址、密钥和可用状态。
- 维护本地模型别名，并将别名映射到一个或多个 Provider。
- 调整每个模型当前运行时的 Provider 顺序。
- 将运行时顺序保存为系统默认启动顺序。
- 查看仪表盘和审计日志，了解流量、成本、错误和延迟情况。

### 3.2 API 调用方

API 调用方是 IDE、Agent、脚本或局域网内其他客户端，负责：

- 使用统一的网关 API Key 访问本系统。
- 通过本地模型别名发起模型列表、聊天补全或 Responses 请求。
- 不直接感知真实上游 Provider 的密钥和模型名。

### 3.3 局域网客户端

当系统开启局域网 API 模式后，局域网客户端可以调用公开的模型接口，但不能访问管理后台。

## 4. 功能需求

### 4.1 OpenAI 兼容网关

系统应提供 OpenAI 兼容的基础能力，至少包括以下三类调用：

- 模型列表查询。
- 聊天补全调用。
- Responses 调用。

网关行为要求如下：

- 所有对外调用都使用统一的网关 API Key 进行鉴权。
- 调用方只需要知道本地暴露的模型别名，不需要知道真实上游模型名。
- 模型请求进入系统后，应根据该模型当前生效的路由规则选择目标 Provider。
- 请求成功时，系统应将结果返回给调用方，并保持调用模式一致，包括普通响应和流式响应。
- 请求失败时，系统应向调用方返回明确错误，并同时留下可检索的审计记录。

兼容性要求如下：

- 应支持较大的请求体，能够承载长文本、较大的多模态内容，以及较大的图片输入数据。
- 对外暴露的模型列表中，只展示当前确实可路由的模型别名。
- 模型列表应体现本地模型标识，并保留展示名信息，方便上层客户端识别。

错误场景要求如下：

- 网关 API Key 不匹配时，应拒绝请求。
- 请求的模型别名未配置，或虽已配置但当前没有可用路由时，应拒绝请求。
- 上游 Provider 正常返回错误时，应将该失败透传为上游错误。
- 上游不可达、网络异常或连接失败时，应返回网络错误。

### 4.2 Provider 管理

系统应提供 Provider 管理能力，用于维护所有上游模型服务。

每个 Provider 至少应包含以下信息：

- Provider 名称。
- Provider 接口地址。
- Provider 真实 API Key。
- 是否启用。
- 连接测试超时时间。

Provider 管理要求如下：

- 管理员可以新增 Provider。
- 管理员可以编辑已有 Provider。
- Provider 名称必须唯一，避免重复配置。
- Provider 可以被启用或停用。
- 已停用的 Provider 不应参与可用模型暴露，也不应参与实际请求路由。

系统应提供 Provider 连通性测试能力，测试结果至少应包含：

- 是否成功。
- HTTP 状态码。
- 响应耗时。
- 上游可见模型数量（若能识别）。
- 错误或提示信息。

补充规则如下：

- 连接测试超时只影响“测试连接”动作，不影响真实业务流量。
- 当前版本没有“删除 Provider”能力，需求基线中不应默认包含此能力。
- 当前版本也没有“全局激活某个 Provider”或“设置全局默认 Provider”的产品能力，路由以模型维度为核心。

### 4.3 模型别名与路由管理

系统应允许管理员定义对外暴露的本地模型，并维护“模型别名 -> Provider 路由”的关系。

每个模型别名至少应包含以下信息：

- 模型别名。
- 展示名称。
- 是否启用。

模型别名规则如下：

- 模型别名必须是可稳定引用的标识。
- 当前命名约束只允许字母、数字、点、下划线、冒号和短横线。

每条模型路由绑定至少应包含以下信息：

- 关联的 Provider。
- 该 Provider 上真实使用的上游模型名。
- 输入价格（按每百万 Tokens）。
- 输出价格（按每百万 Tokens）。
- 默认顺序优先级。
- 是否启用。

路由管理要求如下：

- 一个模型别名可以绑定多个 Provider。
- 同一个模型别名下，同一个 Provider 只能绑定一次。
- 管理员可以新增模型别名。
- 管理员可以修改模型别名和展示名称。
- 管理员可以启用或停用某个模型别名。
- 管理员可以为模型添加 Provider 绑定。
- 管理员可以移除某个模型下的 Provider 绑定。
- 管理员可以移除整个模型别名及其绑定关系。

顺序控制要求如下：

- 每个模型都应维护独立的 Provider 顺序。
- 运行时顺序与默认启动顺序应区分管理。
- 管理员可以先调整当前顺序，再将其应用到正在运行的路由器。
- 管理员可以将当前顺序保存为默认启动顺序，供系统重启后沿用。

模型可用性规则如下：

- 只有当模型别名本身启用、绑定关系启用、对应 Provider 启用时，该模型才算真正可用。
- 对外可见模型列表只应返回满足上述条件的模型。

当前版本的关键边界如下：

- 当前版本按“当前顺序中的第一可用绑定”选择 Provider。
- 当前版本不包含跨 Provider 自动故障转移、自动重试或熔断切换能力。
- 也就是说，如果当前命中的 Provider 请求失败，系统会记录失败并返回错误，而不是自动切换到下一个 Provider。

### 4.4 访问控制与网络暴露

系统应支持两种访问模式：

- 仅本机访问模式。
- 局域网 API 暴露模式。

访问控制要求如下：

- 在仅本机访问模式下，管理后台和 API 都只在本机可访问。
- 在局域网 API 暴露模式下，局域网客户端可以访问公开的模型接口。
- 即使开启局域网 API 暴露模式，管理后台仍必须保持仅本机可访问。
- 非本机来源访问管理后台时，应明确返回“后台仅限本机访问”的提示。

运行信息展示要求如下：

- 管理界面应展示本机管理地址。
- 管理界面应展示本机 API 地址。
- 当启用局域网 API 暴露时，应展示可供局域网客户端使用的 API 地址列表。

### 4.5 仪表盘与运营数据

系统应提供仪表盘，用于观察模型调用量、成本和稳定性。

时间维度要求如下：

- 支持按日查看。
- 支持按周查看。
- 支持按月查看。
- 日视图按小时展示趋势。
- 周视图和月视图按天展示趋势。

总体统计项至少应包括：

- 请求总数。
- 成功数。
- 失败数。
- 错误率。
- 输入 Tokens。
- 输出 Tokens。
- 总 Tokens。
- 预估总成本。
- 平均延迟。
- P50 延迟。
- P95 延迟。
- 缺失 usage 的请求数。

仪表盘应提供以下视图：

- 总体趋势视图。
- 按 Provider 维度的统计视图。
- 按模型维度的统计视图。

每个 Provider / 模型卡片至少应展示：

- 请求数。
- 成功数。
- 失败数。
- 总 Tokens。
- 预估成本。
- 平均延迟。
- P95 延迟。
- 对应时间范围内的趋势图。

当前版本的统计范围边界如下：

- 仪表盘统计以实际推理调用为主，即聊天补全和 Responses 请求。
- 模型列表查询不计入仪表盘的核心趋势统计。

### 4.6 审计日志与问题追踪

系统应提供请求审计日志，用于排障、核对调用结果和分析错误。

每条审计记录至少应包含：

- 请求时间。
- 请求 ID。
- 调用接口类型。
- 命中的 Provider。
- 使用的模型别名。
- 实际上游模型名。
- 是否为流式请求。
- 状态分类。
- HTTP 状态码。
- 响应耗时。
- 输入 Tokens。
- 输出 Tokens。
- 总 Tokens。
- 预估成本。
- 错误代码。
- 错误摘要。

状态分类至少应覆盖：

- 成功。
- 未授权。
- 配置错误。
- 上游错误。
- 网络错误。

审计查询能力要求如下：

- 支持按 Provider 筛选。
- 支持按模型别名筛选。
- 支持按状态筛选。
- 支持按接口类型筛选。
- 支持分页浏览。

在能力边界上，当前版本后端还具备按时间范围过滤审计日志的能力，后续若重做界面，建议保留该筛选维度。

### 4.7 数据留存与隐私保护

系统应保留管理和统计所需的配置与元数据，但避免过度保留敏感内容。

数据留存要求如下：

- 应持久化保存 Provider 配置。
- 应持久化保存模型别名与路由绑定关系。
- 应持久化保存默认顺序和审计元数据。
- 审计记录应保存请求级元信息，便于统计和排障。

隐私要求如下：

- 系统不应保存完整 Prompt 内容。
- 系统不应保存完整响应内容。
- 审计只保留必要的元数据、计量信息和错误摘要。

成本相关要求如下：

- 成本按每条绑定上配置的输入/输出单价进行估算。
- 该成本是运营参考值，不代表真实结算结果。

### 4.8 运行与后台常驻能力

除了产品功能外，当前项目还包含可交付的运行管理能力，适合作为需求范围的一部分保留。

运行方式要求如下：

- 支持开发态运行。
- 支持生产态启动。
- 支持后台常驻运行。

后台服务管理要求如下：

- 支持启动服务。
- 支持停止服务。
- 支持重启服务。
- 支持查看当前运行状态。

自动启动要求如下：

- 支持将系统注册为登录后自动启动任务。
- 支持移除自动启动任务。

运行可观测性要求如下：

- 应保留标准输出日志。
- 应保留错误日志。
- 应保留当前运行状态信息，便于判断服务是否已启动、是否正在启动、端口是否被占用。

局域网模式下的补充要求如下：

- 首次开启局域网 API 暴露时，应保证调用路径具备必要的本机防火墙放行条件。
- 管理员应能明确知道局域网客户端可使用的访问地址。

## 5. 典型业务流程

### 5.1 初次配置流程

- 管理员进入本地后台。
- 新增一个或多个 Provider。
- 对每个 Provider 执行连通性测试。
- 新增一个或多个本地模型别名。
- 为每个模型绑定真实 Provider 和上游模型名。
- 调整模型当前顺序，并保存默认顺序。

### 5.2 日常调用流程

- 客户端使用统一网关 API Key 调用本系统。
- 客户端传入本地模型别名。
- 系统根据该模型当前顺序选择目标 Provider。
- 系统返回推理结果，并记录审计与统计数据。

### 5.3 调优与切换流程

- 管理员发现某个模型的成本、错误率或延迟不理想。
- 管理员在模型路由页调整该模型的 Provider 顺序。
- 管理员先将新顺序应用到当前运行实例。
- 若效果符合预期，再将当前顺序保存为默认启动顺序。

### 5.4 排障流程

- 管理员在仪表盘查看请求量、成功率、延迟和成本。
- 管理员在审计日志中筛选某个 Provider、模型或错误状态。
- 通过错误摘要、HTTP 状态和时间信息定位问题来源。

## 6. 当前版本明确边界

以下内容应被视为“当前版本尚未纳入需求基线”或“暂未支持”：

- 不支持非 OpenAI 兼容协议的上游 Provider。
- 不支持多租户、用户体系或细粒度权限控制。
- 不支持为不同客户端分配不同网关 Key，当前为单一固定网关 Key。
- 不支持 Provider 删除能力。
- 不支持全局默认 Provider 或全局激活 Provider 逻辑。
- 不支持跨 Provider 自动故障转移、自动重试或智能切流。
- 不支持保存完整请求内容和完整响应内容。
- 当前界面未完整暴露所有可筛选维度，例如审计时间范围更多是后端能力而非现有前台主流程。

## 7. 一句话总结

这是一个“模型优先路由”的本地 LLM 网关与管理台：它对外提供统一的 OpenAI 兼容入口，对内让管理员为每个模型配置独立的 Provider 组合、顺序、价格和启停状态，并配套提供局域网暴露、仪表盘统计、审计追踪以及后台常驻运行能力。
