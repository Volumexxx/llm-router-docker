# Docker / NAS 公网版需求文档

## 1. 文档说明

本文档用于定义“LLM 路由转发平台 Docker 版本”的需求基线。

该版本继承本地版的核心目标，即：

- 提供统一的 OpenAI 兼容网关。
- 允许以模型维度管理多个上游 Provider 和路由顺序。
- 提供调用统计、成本估算、延迟分析和审计能力。

但运行环境和暴露方式发生了明显变化：

- 系统将以 Docker 容器形式部署在家用 NAS 上。
- 系统将通过公网域名提供访问能力。
- 至少模型网关能力会对公网开放。
- 运行环境从“本机桌面应用 / 本地常驻服务”转为“长期在线的自托管网络服务”。

因此，Docker 版本除了保留原有路由与管理能力外，还必须补充公网暴露场景下的安全、鉴权、容器化部署、持久化、反向代理适配和运维可观测性要求。

本文档描述的是 Docker 版本的产品需求与运行要求，不展开具体技术实现细节。

## 2. 产品定位

该项目是一个面向自托管场景的 LLM 路由与管理平台，运行在 NAS 或其他服务器的 Docker 环境中，对外提供 OpenAI 兼容接口，并允许管理员通过 Web 管理台维护上游 Provider、模型别名、路由顺序和运营数据。

产品目标如下：

- 对外提供统一的 OpenAI 兼容调用入口，屏蔽上游 Provider 差异。
- 支持在 Docker 中稳定运行，适合长期常驻部署在 NAS 上。
- 支持通过公网域名访问网关接口。
- 支持通过持久化存储保存配置、路由关系和审计数据，避免容器重建导致数据丢失。
- 提供适合公网暴露场景的基础安全能力，包括后台鉴权、密钥管理、限流和日志审计。
- 在不引入复杂多租户体系的前提下，满足“个人 / 家庭 / 小团队自托管”的使用需求。

## 3. 角色与使用场景

### 3.1 管理员

管理员是该 NAS 服务的所有者或维护者，负责：

- 配置上游 Provider 的名称、地址、密钥和可用状态。
- 维护本地模型别名，并将别名映射到一个或多个 Provider。
- 调整每个模型当前运行时的 Provider 顺序。
- 将运行时顺序保存为系统默认启动顺序。
- 配置公网访问相关能力，例如网关密钥、后台登录凭据、域名访问方式和安全策略。
- 查看仪表盘和审计日志，了解流量、成本、错误和延迟情况。

### 3.2 API 调用方

API 调用方是 IDE、Agent、插件、脚本或外部自动化系统，负责：

- 使用统一的网关 API Key 访问系统。
- 通过本地模型别名发起模型列表、聊天补全或 Responses 请求。
- 不直接感知真实上游 Provider 的密钥和模型名。

API 调用方可能来自：

- NAS 所在局域网。
- 公网环境中的个人设备。
- 可访问该公网域名的远程客户端。

### 3.3 外部访问者

Docker 版本需要考虑来自公网的未授权访问行为，包括：

- 未携带网关 Key 的匿名请求。
- 对后台地址的探测与暴力尝试。
- 高频请求、恶意扫描或滥用流量。

系统不要求实现完整安全网关产品能力，但必须具备足以支撑公网暴露的基础防护能力。

## 4. 核心功能需求

### 4.1 OpenAI 兼容网关

系统应提供 OpenAI 兼容的基础能力，至少包括以下三类调用：

- 模型列表查询。
- 聊天补全调用。
- Responses 调用。

网关行为要求如下：

- 所有对外调用都使用统一的网关 API Key 进行鉴权。
- 调用方只需要知道本地暴露的模型别名，不需要知道真实上游模型名。
- 模型请求进入系统后，应根据该模型当前生效的路由规则选择目标 Provider。
- 请求成功时，系统应将结果返回给调用方，并保持调用模式一致，包括普通响应和流式响应。
- 请求失败时，系统应向调用方返回明确错误，并同时留下可检索的审计记录。

兼容性要求如下：

- 应支持较大的请求体，能够承载长文本、较大的多模态内容，以及较大的图片输入数据。
- 应支持流式响应透传，适配长连接和公网访问场景。
- 对外暴露的模型列表中，只展示当前确实可路由的模型别名。
- 模型列表应体现本地模型标识，并保留展示名信息，方便上层客户端识别。

公网暴露补充要求如下：

- 系统应能在反向代理之后正常工作，正确识别协议、域名和客户端来源信息。
- 当通过 HTTPS 域名访问时，系统不应因容器内监听 HTTP 而影响外部正常调用。
- 系统应支持在子域名或独立域名下对外提供 API。

错误场景要求如下：

- 网关 API Key 不匹配时，应拒绝请求。
- 请求的模型别名未配置，或虽已配置但当前没有可用路由时，应拒绝请求。
- 上游 Provider 正常返回错误时，应将该失败透传为上游错误。
- 上游不可达、网络异常或连接失败时，应返回网络错误。
- 当请求触发限流、并发限制或安全策略时，应返回明确的错误信息。

### 4.2 Provider 管理

系统应提供 Provider 管理能力，用于维护所有上游模型服务。

每个 Provider 至少应包含以下信息：

- Provider 名称。
- Provider 接口地址。
- Provider 真实 API Key。
- 是否启用。
- 连接测试超时时间。

Provider 管理要求如下：

- 管理员可以新增 Provider。
- 管理员可以编辑已有 Provider。
- Provider 名称必须唯一，避免重复配置。
- Provider 可以被启用或停用。
- 已停用的 Provider 不应参与可用模型暴露，也不应参与实际请求路由。

系统应提供 Provider 连通性测试能力，测试结果至少应包含：

- 是否成功。
- HTTP 状态码。
- 响应耗时。
- 上游可见模型数量（若能识别）。
- 错误或提示信息。

补充规则如下：

- 连接测试超时只影响“测试连接”动作，不影响真实业务流量。
- 当前版本没有“删除 Provider”能力，需求基线中不应默认包含此能力。
- 当前版本也没有“全局激活某个 Provider”或“设置全局默认 Provider”的产品能力，路由以模型维度为核心。

### 4.3 模型别名与路由管理

系统应允许管理员定义对外暴露的本地模型，并维护“模型别名 -> Provider 路由”的关系。

每个模型别名至少应包含以下信息：

- 模型别名。
- 展示名称。
- 是否启用。

模型别名规则如下：

- 模型别名必须是可稳定引用的标识。
- 当前命名约束只允许字母、数字、点、下划线、冒号和短横线。

每条模型路由绑定至少应包含以下信息：

- 关联的 Provider。
- 该 Provider 上真实使用的上游模型名。
- 输入价格（按每百万 Tokens）。
- 输出价格（按每百万 Tokens）。
- 默认顺序优先级。
- 是否启用。

路由管理要求如下：

- 一个模型别名可以绑定多个 Provider。
- 同一个模型别名下，同一个 Provider 只能绑定一次。
- 管理员可以新增模型别名。
- 管理员可以修改模型别名和展示名称。
- 管理员可以启用或停用某个模型别名。
- 管理员可以为模型添加 Provider 绑定。
- 管理员可以移除某个模型下的 Provider 绑定。
- 管理员可以移除整个模型别名及其绑定关系。

顺序控制要求如下：

- 每个模型都应维护独立的 Provider 顺序。
- 运行时顺序与默认启动顺序应区分管理。
- 管理员可以先调整当前顺序，再将其应用到正在运行的路由器。
- 管理员可以将当前顺序保存为默认启动顺序，供系统重启后沿用。

模型可用性规则如下：

- 只有当模型别名本身启用、绑定关系启用、对应 Provider 启用时，该模型才算真正可用。
- 对外可见模型列表只应返回满足上述条件的模型。

当前版本的关键边界如下：

- 当前版本按“当前顺序中的第一可用绑定”选择 Provider。
- 当前版本不包含跨 Provider 自动故障转移、自动重试或熔断切换能力。
- 如果当前命中的 Provider 请求失败，系统会记录失败并返回错误，而不是自动切换到下一个 Provider。

### 4.4 后台管理与访问控制

Docker 版本不再假设“管理员一定在本机操作”，因此系统必须提供独立的后台鉴权能力。

后台管理要求如下：

- 管理后台必须具备登录鉴权能力，不能仅依赖“来源是本机”这一条件。
- 系统至少应支持单管理员账号的登录方式。
- 管理员密码必须以不可逆摘要形式保存，不能明文存储。
- 登录成功后应建立后台会话，并支持退出登录。
- 后台关键操作应要求具备有效管理身份，未登录状态不得访问管理接口。

公网场景补充要求如下：

- 管理后台默认不应裸露为“无鉴权可访问”状态。
- 若管理员通过公网访问后台，系统仍应保持完整鉴权。
- 后台接口应具备基础防暴力尝试能力，例如登录限频、失败计数或配合反向代理限流。
- 后台会话应适配 HTTPS 反向代理场景，保证 Cookie 或其他会话凭证可以安全工作。

兼容部署要求如下：

- 系统应支持 API 与后台共用同一服务实例。
- 系统应支持通过不同路径前缀或不同域名区分后台与 API。
- 当部署在反向代理后时，后台生成的访问地址、跳转和安全策略应与外部域名一致。

### 4.5 API 密钥与安全策略

公网暴露情况下，单一固定网关 Key 仍可作为当前版本基线，但必须具备基本可维护性。

网关密钥要求如下：

- 系统应支持配置统一的网关 API Key。
- 网关 API Key 不得硬编码在镜像中。
- 网关 API Key 应通过环境变量、挂载配置文件或后台初始化流程注入。
- 系统应支持后续轮换网关 API Key，而不依赖重建所有业务配置。

安全策略要求如下：

- 系统应支持按 IP、路径或接口类别施加基础限流能力，至少应能保护登录接口和公开 API。
- 系统应支持配置最大请求体大小，防止异常超大请求拖垮服务。
- 系统应支持配置请求超时时间和上游超时时间。
- 系统应支持可选的来源白名单或管理后台访问白名单。

当前版本边界如下：

- 当前版本不要求实现多租户密钥体系。
- 当前版本不要求为不同客户端分配不同 API Key。
- 当前版本不要求实现复杂 RBAC 权限模型。

### 4.6 仪表盘与运营数据

系统应提供仪表盘，用于观察模型调用量、成本和稳定性。

时间维度要求如下：

- 支持按日查看。
- 支持按周查看。
- 支持按月查看。
- 日视图按小时展示趋势。
- 周视图和月视图按天展示趋势。

总体统计项至少应包括：

- 请求总数。
- 成功数。
- 失败数。
- 错误率。
- 输入 Tokens。
- 输出 Tokens。
- 总 Tokens。
- 预估总成本。
- 平均延迟。
- P50 延迟。
- P95 延迟。
- 缺失 usage 的请求数。

仪表盘应提供以下视图：

- 总体趋势视图。
- 按 Provider 维度的统计视图。
- 按模型维度的统计视图。

每个 Provider / 模型卡片至少应展示：

- 请求数。
- 成功数。
- 失败数。
- 总 Tokens。
- 预估成本。
- 平均延迟。
- P95 延迟。
- 对应时间范围内的趋势图。

统计范围边界如下：

- 仪表盘统计以实际推理调用为主，即聊天补全和 Responses 请求。
- 模型列表查询不计入仪表盘的核心趋势统计。

### 4.7 审计日志与问题追踪

系统应提供请求审计日志，用于排障、核对调用结果和分析错误。

每条审计记录至少应包含：

- 请求时间。
- 请求 ID。
- 调用接口类型。
- 命中的 Provider。
- 使用的模型别名。
- 实际上游模型名。
- 是否为流式请求。
- 状态分类。
- HTTP 状态码。
- 响应耗时。
- 输入 Tokens。
- 输出 Tokens。
- 总 Tokens。
- 预估成本。
- 错误代码。
- 错误摘要。
- 客户端来源信息。

状态分类至少应覆盖：

- 成功。
- 未授权。
- 配置错误。
- 上游错误。
- 网络错误。
- 触发安全策略。

审计查询能力要求如下：

- 支持按 Provider 筛选。
- 支持按模型别名筛选。
- 支持按状态筛选。
- 支持按接口类型筛选。
- 支持按时间范围筛选。
- 支持分页浏览。

公网版本补充要求如下：

- 当请求来自公网时，应尽量记录真实客户端来源地址，前提是部署链路能够提供可信代理头。
- 审计记录中不应泄露完整密钥、完整 Prompt 或完整响应内容。
- 对于登录失败、未授权访问和异常扫描行为，应保留必要审计痕迹。

### 4.8 数据留存与隐私保护

系统应保留管理和统计所需的配置与元数据，但避免过度保留敏感内容。

数据留存要求如下：

- 应持久化保存 Provider 配置。
- 应持久化保存模型别名与路由绑定关系。
- 应持久化保存默认顺序和审计元数据。
- 应持久化保存管理员账号信息和安全相关配置。
- 审计记录应保存请求级元信息，便于统计和排障。

隐私要求如下：

- 系统不应保存完整 Prompt 内容。
- 系统不应保存完整响应内容。
- 审计只保留必要的元数据、计量信息和错误摘要。
- 上游 API Key、网关 API Key 和管理员密码不得以明文形式出现在导出日志或调试输出中。

成本相关要求如下：

- 成本按每条绑定上配置的输入/输出单价进行估算。
- 该成本是运营参考值，不代表真实结算结果。

### 4.9 Docker 部署与持久化

Docker 版本应把“可稳定部署和可恢复运行”视为核心需求，而不是附加能力。

镜像与运行要求如下：

- 系统应提供适合 Docker 运行的单容器服务形态。
- 容器应支持前台启动，适配 Docker 的常规生命周期管理。
- 容器日志应输出到标准输出和标准错误，方便由 Docker 或 NAS 平台接管。
- 镜像应尽量精简，避免明显不必要的运行时依赖。
- 若条件允许，应优先支持 `amd64` 和 `arm64` 多架构镜像，以适配不同 NAS 硬件。

持久化要求如下：

- 配置数据必须落在可挂载的数据目录或持久化卷中。
- 容器重建、更新镜像或重启后，已配置的 Provider、模型路由、审计数据和安全配置不应丢失。
- 应允许将数据库文件、日志目录或导出目录映射到宿主机。

环境配置要求如下：

- 关键运行参数应支持通过环境变量配置。
- 系统至少应支持配置监听端口、数据目录、网关 Key、管理员初始化凭据、时区和基础安全参数。
- 缺少关键配置时，应给出明确启动错误，而不是静默运行在不安全状态。

### 4.10 反向代理与公网域名适配

由于系统运行在 NAS Docker 环境中，并通过公网域名访问，因此必须适配常见反向代理链路。

适配要求如下：

- 系统应支持运行在 Nginx、Caddy、Traefik 或 NAS 自带反代之后。
- 系统应正确处理 `X-Forwarded-For`、`X-Forwarded-Proto`、`X-Forwarded-Host` 等可信代理头。
- 系统应支持配置外部访问基地址，避免后台页面、跳转地址和接口文档生成错误链接。
- 当外部使用 HTTPS 而容器内部为 HTTP 时，系统应仍能正确生成安全链接和会话策略。

部署展示要求如下：

- 管理界面应展示当前推荐的外部 API 访问地址。
- 如已配置后台外部访问地址，也应明确展示。
- 当系统检测到代理头和配置不一致时，应提供提示，帮助管理员排查反代配置错误。

### 4.11 健康检查与可观测性

系统应具备适合容器编排和长期运行的基础可观测能力。

要求如下：

- 系统应提供健康检查接口，至少区分“服务存活”和“服务可用”。
- 启动阶段应能反映初始化状态，避免容器在未准备完成时被误判为可用。
- 系统应保留标准输出日志和错误日志。
- 启动日志中应明确当前监听地址、数据目录、代理模式和关键安全配置状态，但不得输出敏感密钥明文。
- 应保留当前运行状态信息，便于判断服务是否已启动、是否正在启动、配置是否缺失。

### 4.12 备份与恢复

由于该系统部署在个人 NAS 上，管理员通常需要在升级、迁移或设备故障时恢复服务，因此应具备基础备份恢复约束。

要求如下：

- 持久化数据目录应具备清晰结构，便于直接备份。
- 系统升级不应破坏已有数据格式，若存在迁移步骤，应在启动时自动执行或明确提示。
- 应允许管理员导出关键配置或直接通过卷备份方式恢复。
- 恢复后系统应保留模型路由、Provider 配置、后台账号和审计数据。

## 5. 典型业务流程

### 5.1 初次部署流程

- 管理员在 NAS 上创建持久化目录。
- 管理员通过 Docker 或 Docker Compose 启动服务。
- 管理员配置公网域名反向代理，并确保外部 HTTPS 访问可用。
- 管理员完成后台初始化账号和网关 Key 配置。
- 管理员进入后台，新增一个或多个 Provider。
- 管理员对每个 Provider 执行连通性测试。
- 管理员新增一个或多个本地模型别名。
- 管理员为每个模型绑定真实 Provider 和上游模型名。
- 管理员调整模型当前顺序，并保存默认顺序。

### 5.2 日常调用流程

- 客户端通过公网域名或局域网地址访问网关接口。
- 客户端携带统一网关 API Key 调用系统。
- 客户端传入本地模型别名。
- 系统根据该模型当前顺序选择目标 Provider。
- 系统返回推理结果，并记录审计与统计数据。

### 5.3 远程管理流程

- 管理员通过后台地址访问管理台。
- 管理员完成登录鉴权。
- 管理员修改 Provider、模型、路由顺序或安全配置。
- 系统即时应用允许热更新的配置，并记录必要审计信息。

### 5.4 调优与切换流程

- 管理员发现某个模型的成本、错误率或延迟不理想。
- 管理员在模型路由页调整该模型的 Provider 顺序。
- 管理员先将新顺序应用到当前运行实例。
- 若效果符合预期，再将当前顺序保存为默认启动顺序。

### 5.5 排障流程

- 管理员在仪表盘查看请求量、成功率、延迟和成本。
- 管理员在审计日志中筛选某个 Provider、模型、状态、接口类型或时间范围。
- 通过错误摘要、HTTP 状态、来源信息和时间信息定位问题来源。

### 5.6 升级与恢复流程

- 管理员备份持久化目录或快照。
- 管理员拉取新镜像并重建容器。
- 系统自动加载既有配置和数据。
- 如存在数据迁移，系统在启动时完成迁移并输出明确日志。
- 若升级失败，管理员可基于备份卷回滚到上一版本。

## 6. 当前版本明确边界

以下内容应被视为“当前版本尚未纳入需求基线”或“暂未支持”：

- 不支持非 OpenAI 兼容协议的上游 Provider。
- 不支持多租户、组织空间或复杂成员协作体系。
- 不支持为不同客户端分配不同网关 Key。
- 不支持细粒度 RBAC 权限系统。
- 不支持 Provider 删除能力。
- 不支持全局默认 Provider 或全局激活 Provider 逻辑。
- 不支持跨 Provider 自动故障转移、自动重试或智能切流。
- 不支持保存完整请求内容和完整响应内容。
- 不要求内建 WAF、验证码、人机校验或专业级入侵防护体系。
- 不要求内建 TLS 证书申请能力，HTTPS 可由外部反向代理终止。

## 7. 与本地版的关键差异

相较于本地版，本版本最核心的变化包括：

- 运行形态从本地常驻服务转为 Docker 容器化服务。
- 部署环境从本机转为 NAS，自带公网域名访问前提。
- API 暴露范围从“本机 / 局域网优先”转为“可公网访问”。
- 后台管理不再依赖本机来源限制，而必须具备独立登录鉴权。
- 新增反向代理适配、持久化卷、健康检查、环境变量配置和备份恢复要求。
- 新增公网暴露下的限流、登录保护、客户端来源记录和敏感信息保护要求。

## 8. 一句话总结

这是一个适合部署在 NAS Docker 环境中的自托管 LLM 路由网关：它对外提供统一的 OpenAI 兼容公网入口，对内允许管理员维护 Provider、模型别名和模型级路由顺序，并以“可公网使用、可长期运行、可持久化、可审计、具备基础安全防护”为 Docker 版本的核心要求。
